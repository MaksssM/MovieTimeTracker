<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/watchedFragment"
            android:icon="@drawable/ic_watched_list" <!-- Посилання на іконку -->
        android:title="Переглянуті" />
        <item
            android:id="@+id/trendingFragment"
            android:icon="@drawable/ic_trending" <!-- Посилання на іконку -->
        android:title="В тренді" />
        <item
            android:id="@+id/settingsFragment"
            android:icon="@drawable/ic_settings" <!-- Посилання на іконку -->
        android:title="Налаштування" />
    </group>
</menu>